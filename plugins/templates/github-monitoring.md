# GitHub monitoring

Use the continued-automation skill. Ask what the user wants monitored, verify available accounts and connector capabilities, then create and preview a draft. Reuse information the user already supplied. No-match checks should use no model. Do not enable a schedule merely because the plugin is installed. Review outputs stay local unless the user requests sharing.
