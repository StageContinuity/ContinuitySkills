# Work knowledge

Use the knowledge-base skill to organize the user's chosen local sources. Agree on scope, preserve provenance and distinguish reusable rules from historical notes. Create a local artifact the user can read and reuse in another session. Cloud upload and rule application require a separate explicit request.
